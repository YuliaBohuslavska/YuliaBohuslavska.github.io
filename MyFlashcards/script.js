/*

let card = document.querySelectorAll('.card');
let cardFront = document.querySelectorAll('.card-front');
let cardBack = document.querySelectorAll('.card-back');
for (let i = 0; i < card.length; i++) {
    cardFront[i].onclick = function () {
        card[i].style.transform = 'rotateY(180deg)';
        cardFront[i].style.visibility = 'hidden';
        cardBack[i].style.visibility = 'visible';
    }
    cardBack[i].onclick = function () {
        card[i].style.transform = 'rotateY(180deg)';
        cardBack[i].style.visibility = 'hidden';
        cardFront[i].style.visibility = 'visible';
    }
}

*/
